// THEIRS.swift — methods reordered (multiply moved above add)
class Calculator {
    func multiply(_ a: Int, _ b: Int) -> Int {
        return a * b
    }

    func add(_ a: Int, _ b: Int) -> Int {
        return a + b
    }
}
